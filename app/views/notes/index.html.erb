<h1><%= Type.find(params[:type_id]).name %></h1>

<strong>hoorah</strong>

<p>
 
<table>
  <% @notes.each do |note| %>
    <tr>
      <td>I'm trying here.</td>
      <td><%= note.type_id %></td>
      <td><%= note.title %></td>
      <td><%= note.text %></td>
      <td><%= link_to 'Show', type_note_path(note.id) %></td>
      <td><%= link_to 'Edit', edit_type_note_path(note.id) %></td>
      <td><%= link_to 'Mark Done', type_note_path(note.id),
              method: :delete,
              data: { confirm: 'I promise I\'m not lying. I\'m really done.' } %></td>
        </tr>
  <% end %>
</table>
<p>
 <%= link_to 'Add another Note', new_type_note_path(Type.find(params[:type_id]).id)%>